<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapepr 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="adminMapper">
	<resultMap type="QnA" id="qnaResultSet">
		<id property="qnano" column="QnA_NO" />
		<result property="qcontent" column="QCONTENT" />
		<result property="qdate" column="QDATE" />
		<result property="qreply" column="QREPLY" />
		<result property="qstatus" column="QSTATUS" />
		<result property="usno" column="US_NO" />	
		<result property="usname" column="US_NAME" />	<!-- USER 테이블 조인을 위한 필드 -->
	</resultMap>
	
	<select id="selectAdminQna" resultMap="qnaResultSet">
		SELECT *
		  FROM
              (SELECT "QnA_NO", QCONTENT, US_NAME
               FROM "QnA", "USER"
               WHERE  "QnA".US_NO = "USER".US_NO
               ORDER BY "QnA_NO")
		 WHERE 
		       ROWNUM <![CDATA[<=]]> 5;
	</select>		
</mapper>